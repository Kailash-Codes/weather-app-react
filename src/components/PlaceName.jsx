import { Typography } from "@mui/material";
import React from "react";

const PlaceName = ({ placeName }) => {
  return <Typography variant="h3">{placeName}</Typography>;
};

export default PlaceName;
